#!/bin/sh

set -e

#DEBHELPER#

#set default theme for the gdm login screen
if [ "$1" = "configure" ] && dpkg --compare-versions "$2" lt 0.38; then
    su -s /bin/sh gdm <<EOF
xfconf-query --channel xsettings --property /Net/ThemeName --set MurrinaStormCloud
EOF
fi
